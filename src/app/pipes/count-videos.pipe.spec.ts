import { CountVideosPipe } from './count-videos.pipe';

describe('CountVideosPipe', () => {
  it('create an instance', () => {
    const pipe = new CountVideosPipe();
    expect(pipe).toBeTruthy();
  });
});
